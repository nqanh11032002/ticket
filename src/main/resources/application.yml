spring:
  cloud:
    loadbalancer:
      ribbon:
        enabled: false
    gateway:
      routes:
        - id: UserService
          uri: localhost:8085
          predicates:
            - Path=https://user.up.railway.app/api/user/**
  application:
    name: gateway

server:
  port: 8082