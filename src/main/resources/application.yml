spring:
  cloud:
    loadbalancer:
      ribbon:
        enabled: false
    gateway:
      routes:
        - id: ticket-service
          uri: https://ticket.up.railway.app/api/ticket
          predicates:
            - Host=**.ticket.railway.app
        - id: ticket-service
          uri: https://ticket.up.railway.app/api/payment
          predicates:
            - Host=**.ticket.railway.app
        - id: user-service
          uri: https://user.up.railway.app/api/user
          predicates:
            - Host=**.user.railway.app
  application:
    name: gateway

server:
  port: 8082
