spring:
  cloud:
    loadbalancer:
      ribbon:
        enabled: false
    gateway:
      routes:
        - id: UserService
          uri: localhost:8085
          predicates:
            - Path=/api/user/**
            - Host=user.up.railway.app
  application:
    name: gateway

server:
  port: 8082