spring:
  cloud:
    loadbalancer:
      ribbon:
        enabled: false
    gateway:
      routes:
        - id: ticket-service
          uri: https://ticket.up.railway.app
          predicates:
            - Host=www.railway.app
  application:
    name: gateway

server:
  port: 8082