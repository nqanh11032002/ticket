spring:
  cloud:
    loadbalancer:
      ribbon:
        enabled: false
    gateway:
      routes:
        - id: UserService
          uri: https://ticket.up.railway.app/api/**
          predicates:
            - Host=https://railway.app/project/8fe999fe-3a8e-4c5d-9f0c-71a2122275f8
  application:
    name: gateway

server:
  port: 8082